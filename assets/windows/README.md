# Icono de Windows

Si queres personalizar el icono del ejecutable, coloca un archivo:

- `assets/windows/app.ico`

La build con PyInstaller lo va a tomar automaticamente si existe.

Si no existe, la aplicacion igual se empaqueta usando el icono por defecto.
